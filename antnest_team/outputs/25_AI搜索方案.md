# 25 antNest AI 搜索方案

- 负责人：AI 搜索算法/架构工程师（Agent 4）

## M0 现状
检索能力以最小形态内嵌于 Harness 工具集：list_dir 定位 + shell(grep/find)
关键词检索，已支撑演示任务完成。

## M1+ 方案
- LLM 原生检索：query 理解（改写/意图分类）→ 混合召回（稠密+稀疏）→ 重排
- 作为 Harness 工具：search(query) 返回结构化证据（含引用），供 Agent 引用溯源
- 质量：自动化评估（命中率/新鲜度）+ badcase 挖掘闭环，成本与延迟双预算约束
