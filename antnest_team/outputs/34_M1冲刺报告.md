# 34 antNest M1 冲刺报告（模型-Harness 共同进化闭环打通）

- 负责人：AI 产品经理（Agent 17）汇总；全团队执行
- 对应 33 号方案"90 天行动计划"第 1-4 周内容，实际于一个冲刺内完成

## 一、本冲刺五大成果

### 1. 语料扩展 v2（Agent 3）
- v1（32 份 JD）42,673 字符 → v2（JD + 33 份团队交付物 + 产品源码快照）79,242 字符（1.86×）
- "蚁巢用自己铸造自己"数据闭环成立：团队产出回流为训练语料

### 2. 预训练 v2（Agent 24/12，落实 23 号优化意见）
- 620.9K 参数 TinyGPT（词表 1,382），240 步 / 39s（CPU）
- **warmup + 余弦退火** + **验证集择优保存**（两项均为 23 号建议的落地）
- best val loss **3.8395**（M0 为 4.0769，且任务更难——语料更大、词表更广）

### 3. SFT 后训练（Agent 25，M1 首次启动后训练线）
- 40 条指令（32 职位 QA + 4 知识 QA + 4 动作格式），assistant 区间掩码监督
- 120 epoch / 31s，指令 train loss 4.06 → 0.011，held-out best val **1.182**
- 动作协议写入模型：SFT 数据含 ```action JSON 示例，模型习得 Harness 动作格式

### 4. antNest LLM 本地直连 Harness（Agent 5，核心里程碑）
- 新增 `AntNestLLMClient`（antnest_harness/llm.py）：加载自训 checkpoint 直驱 Agent Loop
- 脚手架解码：模型采样提议优先，不可解析时安全路由兜底（mini 模型专属策略）
- **实跑成功**（demo2）：本地 SFT 模型驱动 Loop 完成
  list_dir → write_file → finish，产出 artifacts/harness_m1_report.md
- 至此三层 LLM 就绪：MockLLM（测试）/ **antNest LLM（本地）** / OpenAI 兼容（真实）

### 5. 质量门禁 M1（Agent 8，累计 14/14 通过）
新增 5 项：语料扩展 / SFT 掩码正确性 / 长 prompt 生成 / **直连端到端** / 词表一致性

## 二、Badcase 闭环实录（第 2 轮，Agent 8）
| # | 现象 | 根因 | 修复 |
|---|------|------|------|
| ① | SFT 崩溃 IndexError | SFT 序列 256 > 模型 ctx 192，位置编码越界 | SFT 长度对齐模型配置 |
| ② | 复训仍崩溃 | **词表漂移**：训练后新增源码入料，新 tokenizer ≠ checkpoint 词表 | 词表版本锁定（强制用 v2_vocab.json + 一致性断言+测试） |
| ③ | val loss 恒 0.0000 | 验证集 4 条 < batch 16，评估循环空转 | 分批允许末尾小批 |

三条均已修复并回归（含新增防回归测试 test_ckpt_vocab_consistency）。

## 三、诚实评估（Agent 2/23 联合）
- SFT 的 val 1.182 表明指令格式已习得，但采样仍见重复退化（"ant ant LLM…"）——
  620K 字符级模型的能力天花板明确，属于**管线验证件**而非能力件
- 直连演示中 finish 动作来自脚手架路由的概率较高——模型独立决策力待语料千倍扩容后复测
- 结论：M1 目标（闭环打通）已达成；能力目标留给 M2（外部语料 + BPE + 更大模型）

## 四、M2 建议（下个冲刺）
1. 外部语料接入（合规框架依 05 号），目标 ≥10M 字符
2. BPE tokenizer（词表 8K 起），建立 vocab 版本注册表
3. RL 环境：以 14/15 号评测通过率为奖励（Agent 18/19 已备好任务定义）
4. Harness：MCP/Skills 扩展 + 评测集驱动迭代
